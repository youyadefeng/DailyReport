{
  "name": "ai-news-pipeline",
  "private": true,
  "type": "module",
  "scripts": {
    "run:pipeline": "node src/bootstrap.mjs",
    "run:pipeline:verbose": "node src/bootstrap.mjs --verbose",
    "run:pipeline:github": "node src/bootstrap.mjs --source github",
    "run:pipeline:github:verbose": "node src/bootstrap.mjs --source github --verbose"
  },
  "devDependencies": {
    "@openai/codex": "^0.114.0"
  }
}
